#include <stdio.h>
#include <errno.h>

int main() {
    char currentLine[5] = "Hello";                // Somewhere to put the retrieved data...
    printf(currentLine);
    if (*currentLine = "H"[0]) {
        printf("I SHALL NOT BE BOUND!");
    }
}